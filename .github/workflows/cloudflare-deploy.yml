# Deploy to Cloudflare Pages
#
# Required GitHub Secrets:
# - CLOUDFLARE_API_TOKEN: API Token with "Cloudflare Pages — Edit" permission (NOT Global API Key)
# - CLOUDFLARE_ACCOUNT_ID: Your Cloudflare account ID
# - CLOUDFLARE_PROJECT_NAME: Your Cloudflare Pages project name
#
# For setup instructions, see: .github/CLOUDFLARE_SETUP.md
#
name: Deploy to Cloudflare Pages

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      deployments: write
      pull-requests: write
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Create GitHub Deployment
        id: deployment
        uses: actions/github-script@v7
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          script: |
            const deployment = await github.rest.repos.createDeployment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: context.ref,
              environment: context.eventName === 'pull_request' ? 'preview' : 'production',
              auto_merge: false,
              required_contexts: [],
              description: context.eventName === 'pull_request' 
                ? `Deploy PR #${context.payload.pull_request.number}` 
                : 'Deploy to production'
            });
            return deployment.data.id;

      # Deploy to Cloudflare Pages using Wrangler
      # IMPORTANT: CLOUDFLARE_API_TOKEN must be an API Token (not Global API Key)
      # with "Cloudflare Pages — Edit" permission
      # See .github/CLOUDFLARE_SETUP.md for setup instructions
      - name: Publish to Cloudflare Pages
        id: cloudflare
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy out --project-name=${{ secrets.CLOUDFLARE_PROJECT_NAME }}
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}

      - name: Update Deployment Status (Success)
        if: success()
        uses: actions/github-script@v7
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DEPLOYMENT_ID: ${{ steps.deployment.outputs.result }}
          CLOUDFLARE_URL: ${{ steps.cloudflare.outputs.deployment-url }}
          CLOUDFLARE_ALIAS: ${{ steps.cloudflare.outputs.pages-deployment-alias-url }}
        with:
          script: |
            const deploymentId = process.env.DEPLOYMENT_ID;
            const cloudflareUrl = process.env.CLOUDFLARE_URL || process.env.CLOUDFLARE_ALIAS;
            
            await github.rest.repos.createDeploymentStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              deployment_id: deploymentId,
              state: 'success',
              environment_url: cloudflareUrl,
              log_url: `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
              description: 'Deployment successful'
            });

      - name: Update Deployment Status (Failure)
        if: failure() && steps.deployment.outputs.result
        uses: actions/github-script@v7
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DEPLOYMENT_ID: ${{ steps.deployment.outputs.result }}
        with:
          script: |
            const deploymentId = process.env.DEPLOYMENT_ID;
            
            await github.rest.repos.createDeploymentStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              deployment_id: deploymentId,
              state: 'failure',
              log_url: `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
              description: 'Deployment failed'
            });

      - name: Comment on PR
        if: github.event_name == 'pull_request' && success()
        uses: actions/github-script@v7
        env:
          CLOUDFLARE_URL: ${{ steps.cloudflare.outputs.deployment-url }}
          CLOUDFLARE_ALIAS: ${{ steps.cloudflare.outputs.pages-deployment-alias-url }}
        with:
          script: |
            const cloudflareUrl = process.env.CLOUDFLARE_URL || process.env.CLOUDFLARE_ALIAS;
            if (cloudflareUrl) {
              github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.payload.pull_request.number,
                body: `## 🚀 Cloudflare Pages Deployment\n\n**Preview URL:** ${cloudflareUrl}\n\nDeployed successfully!`
              });
            }
